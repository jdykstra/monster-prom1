IDE Test build:

dd if=pm_sys/system.wrk.code of=prom1.bin skip=1 count=4

Normal Build:

ucsdpsys -P "II.0" -w disk-images/BLDSYS/ -r monster-prom1/src/ -w disk-images/SCRTCH -b script1

dd if=pm_sys/system.wrk.code of=prom1.bin skip=1 count=8

Delete 0X800 zero bytes generated by the .ORG to KEY_TBL.

